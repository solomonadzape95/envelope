/**
 * Ensures the .envelope directory exists in the current project root
 * and returns the path to it
 */
export declare function ensureProjectEnvelopeDir(): string;
/**
 * Gets the path to a file within the project's .envelope directory
 */
export declare function getProjectEnvelopePath(filename: string): string;
//# sourceMappingURL=project-envelope.d.ts.map